@model IEnumerable<WebApplication3.JobCandidate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Resume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isDeleted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.NationalIDNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Resume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isDeleted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.NationalIDNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobCandidateID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobCandidateID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobCandidateID })
        </td>
    </tr>
}

</table>
